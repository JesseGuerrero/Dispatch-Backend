function confirmDelete() {
    //check if confirmation-text is hidden
        // make a 5 second wait then show confirmation-text
    // else
        // delete the newsletter
        // logout
        // redirect to login
}